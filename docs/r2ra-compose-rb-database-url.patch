diff --git a/deploy/docker-compose.eb.yml b/deploy/docker-compose.eb.yml
index dad584d..a60a9a4 100644
--- a/deploy/docker-compose.eb.yml
+++ b/deploy/docker-compose.eb.yml
@@ -30,6 +30,9 @@ services:
     environment:
       CORS_ORIGINS: ${CORS_ORIGINS:-*}
       GEMINI_API_KEY: ${GEMINI_API_KEY:-}
+      # Builder's own database on r2ra-aurora (role2-builder docs/REVIEW_STORAGE.md).
+      # Deliberately NOT R2RA's DATABASE_URL; empty = builder runs without storage.
+      DATABASE_URL: ${RB_DATABASE_URL:-}
     restart: always
     logging:
       driver: json-file
